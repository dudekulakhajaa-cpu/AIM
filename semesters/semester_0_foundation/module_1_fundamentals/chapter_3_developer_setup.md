# Chapter 3 â€” Developer Setup

Set up your workspace tools to write, compile, debug, and track code like a professional.

## ðŸ› ï¸ Essential Toolchain

### 1. Visual Studio / MSVC Compiler
- Install **Visual Studio Community Edition** (use current standard).
- Select the **Desktop development with C++** workload.
- Installs the MSVC compiler, debugger, and standard libraries.

### 2. Git & GitHub
- Git tracks history and supports parallel branching.
- Setup GitHub account for portfolio hosting.
- Basic commands:
  ```bash
  git init
  git add .
  git commit -m "Commit message"
  git push origin main
  ```

### 3. VS Code
- Installed as a fast code browser, text editor, and markdown viewer.
- Recommended extensions:
  - C/C++ (Microsoft)
  - GitLens
  - Markdown All in One

---

## ðŸš€ Practical Assignment: Environment Check
1. Open PowerShell and run: `git --version` to verify git installation.
2. Compile and run your first Hello World C++ file in Visual Studio or CLI.